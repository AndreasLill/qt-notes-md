import QtQuick
import QtQuick.Controls
import qtnotesmd

Menu {
    id: root
    padding: 4

    background: Rectangle {
        implicitWidth: 200
        implicitHeight: 30
        radius: 4
        color: Theme.color.overlay
    }

}